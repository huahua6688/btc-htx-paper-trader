# 信号信息量审计（Signal Information Audit）

这是一个**在写策略之前**跑的检查，不是又一个策略。

## 为什么要有这个东西

V1.2、V2、V3、V4 是同一个做法做出来的：定一批技术指标规则，搜参数，挑回测收益最高的那组。

这个做法有一个结构性缺陷：**不管数据里有没有规律，它都一定会挑出一个好看的 winner。**
本仓库已经用可复现的实验证明过这一点 —— 128 个零优势的随机候选，best-of-128 平均能回测出
+30 单位、100% 为正。

对照实际结果：

| 策略 | 零成本下的毛收益 | 状态 |
| --- | --- | --- |
| V1.2（冻结 Champion） | —— | 实盘在亏 |
| research-v2 | −4.2% | 成本前就是负的 |
| multi-venue-v3 | −5.3% | 成本前就是负的 |
| breakout-v4 | +7.1% | 唯一为正，但样本只有 28 笔，落在选择偏差的噪音带里 |

V1.2 当年正是被这个流程挑出来的。所以问题不在「参数没调好」，在**方法本身**。
照同样的方式再做一个 V5，结果只会一样。

## 这个审计问的是另一个问题

不设入场、不设止损、不算仓位、不做资金管理，只问：

> 特征 X 在 t 时刻的取值，和 t 之后 k 根 K 线的收益，有没有关系？
> 关系有多大？扣掉手续费、滑点、资金费之后还剩不剩？

自由度极少 —— 没有阈值、没有出场规则、没有仓位曲线可以调，所以结果是可证伪的。

## 怎么跑

数据目录要先有（服务器换过就得重新拉一次，K 线加资金费，几十分钟）：

```bash
npm run data:update -- --from=2024-09-01T00:00:00.000Z --to=2026-07-31T23:45:00.000Z
```

然后：

```bash
npm run research:signal-audit
```

可选参数：

- `--null-samples=3000`：零分布抽样次数，默认 3000。18.5 万根 K 线约 1 分钟。
- `--seed=20260918`：随机种子。同一份数据同一个种子，结果逐字节可复现。
- `--buckets=10`：分位数，默认十分位。
- `--alpha=0.05`：显著性水平。
- `--catalog=<目录>`：指定数据目录。

终端只打结论和最强的几个检验；44 次检验各带 10 个分位的完整结果写在 `reportPath` 指向的 JSON 里。

## 结论只有四种

| 结论 | 含义 | 该做什么 |
| --- | --- | --- |
| `TRADABLE_CANDIDATE` | 有特征通过族系检验，且扣成本后仍为正 | **唯一**值得据此写策略的情形 |
| `STATISTICAL_ONLY_NOT_TRADABLE` | 统计上显著，但扣掉成本后为负 | 关系是真的，钱赚不到。别在这上面写策略 |
| `NO_DETECTABLE_EDGE` | 这些特征、这个时间尺度、这个成本水平下测不到 | 不要再做 V5。换时间尺度、换特征、或者换问题 |
| `UNDERPOWERED_NULL_SAMPLING` | 采样次数不足以分辨族系 p 值 | 这不是结论，是一次没做成的检验。加大 `--null-samples` 重跑 |

登记到 research registry 时：`TRADABLE_CANDIDATE` 记 `PASSED`，
`UNDERPOWERED_NULL_SAMPLING` 记 `BLOCKED`，其余记 `PARTIAL`。

## 三个必须一起看的量

缺任何一个，结论就是错的。

1. **IC（Spearman 秩相关）** —— 关系的方向与强度。
2. **p 值** —— 但**不是教科书 t 检验**。重叠的前向收益有极强自相关，
   标准 t 统计量会在纯噪音上宣布显著。这里用**循环平移**构造零假设分布：
   平移破坏「特征↔收益」的对应关系，同时保留两条序列各自的自相关结构。
3. **成本后的分位收益** —— IC 可以统计显著却经济上毫无价值。
   一个 0.05% 的分位价差，在 0.14% 的来回成本面前是负的。

## 三条防自欺的设计

**负对照。** ADX、ATR%、成交量比这些**本身不含方向信息**的特征也一起进检验。
如果它们被判显著的数量超出名义水平，说明方法坏了，本次其余结论一律不可用。
注意：alpha=0.05 的检验本来就该让 5% 的负对照「显著」，所以判据是二项尾概率，
不是「出现一个就报警」。

**多重检验用 Westfall–Young minP，不用 BH，也不用比原始 IC 的 max-T。**

- 不用 BH：BH 要求原始 p < alpha/N，而原始 p 的下限是 1/(采样次数+1)。
  检验一多，BH 的结果就被采样次数顶死在远大于 alpha 的地方，再真的优势也判不出来。
- 不用比原始 IC 的 max-T：各特征的零分布宽度差一个数量级。`emaSpreadPct` 这类
  极强自相关的慢特征，循环平移后仍能靠巧合对上长段同向行情，它自己的噪音带就有 0.15；
  而 `momentum4Pct` 的噪音带只有 0.03。用一把尺子量所有检验，等于让慢特征的噪音
  把快特征的真信号整个盖掉 —— 实测中这会把一个**注入的真实优势**判成「没有」。
- minP：每个检验先跟自己的零分布比换算成各自的 p 值，再在每一批平移里取所有检验的
  最小 p 构成族系零分布。尺度差异被归一掉，而共用同一批平移量又让特征之间的相关性自动计入。

**采样精度必须说清楚。** 族系 p 是一个用有限次抽样估出来的比例，
蒙特卡洛标准误约 `sqrt(p(1-p)/n)`。落在 alpha 两倍标准误以内的判定会被列进
`precision.borderlineTests` —— 换个种子就可能翻面，不允许它冒充一个干脆的结论。

## 这个工具能不能检出真东西

`test/signal-audit.test.mjs` 里有两条对着来的测试，缺一不可：

- **校准**：12 条纯随机游走路径上，p 值必须近似均匀（中位数接近 0.5，p<0.05 的比例接近 5%），
  族系误报率必须接近名义水平。零分布偏窄的话这条会红 —— 那正是「在噪音上宣布优势」的失败模式。
- **检出力**：在合成数据里注入 AR(1) phi=0.35 的真实动量优势，审计**必须**认出来，
  而且认出来的必须正是被注入的那一个特征和周期。否则这就是一台只会说「没有」的机器，
  和没有一样。

另外 `buildAuditFeatures` 有一条 point-in-time 测试：把 K 线加长，
已经算出来的历史特征值不能有任何变化。序列版指标也逐值钉死在 `src/indicators.mjs` 上，
否则审计测的就不是策略在用的那个指标。

## 诚实边界

- 只测线性单调关系。真实优势若是条件性的（只在某种状态下成立），这里会被稀释成接近 0。
- 前向收益按固定持有期计算，不含止损止盈。给的是**信号本身的信息量**，不是某个策略的收益。
- 分位收益的成本按平均资金费计入，不是逐笔路径精确值；短周期上这项很小，24h 上不可忽略。
- 循环平移零分布保留各自自相关，但无法保留两条序列间可能存在的共同外生驱动；
  这会让 p 值偏保守而非偏乐观。
- `NO_DETECTABLE_EDGE` 的含义是「这些特征、这个时间尺度、这个成本水平下测不到」，
  **不等于**「市场不可预测」。
- 本项目为 Paper-only：本审计不构成下单建议，也不是「可以上线」的证据。
